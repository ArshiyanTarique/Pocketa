import type { AccountClass } from '../core/types';

/** Category classes a user can create and manage themselves. */
export const CREATABLE_CATEGORY_CLASSES: AccountClass[] = ['expense_category', 'income_category'];
